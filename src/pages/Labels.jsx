import { LabelListContainer, SubHeader } from 'components';

const Labels = () => {
  return (
    <section>
      <SubHeader />
      <LabelListContainer />
    </section>
  );
};

export default Labels;
